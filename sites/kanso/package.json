{
  "name": "@wplite/kanso",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wp-lite build",
    "dev": "wp-lite dev",
    "apply": "wp-lite apply",
    "pull": "wp-lite pull",
    "verify": "wp-lite verify --json",
    "eject": "wp-lite eject",
    "wp-env:start": "npm run build && npx wp-env start",
    "wp-env:stop": "wp-env stop",
    "wp-env:destroy": "wp-env destroy",
    "seed": "wp-lite seed",
    "rebuild": "npm run build && npm run seed"
  },
  "dependencies": {
    "@wplite/compiler": "*"
  },
  "devDependencies": {
    "@wordpress/env": "^11.2.0"
  }
}
