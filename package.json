{
  "name": "wplite",
  "private": true,
  "workspaces": ["packages/*", "sites/*"],
  "scripts": {
    "site": "node scripts/site.mjs",
    "build:all": "npm run build --workspaces --if-present",
    "apply:all": "npm run apply --workspaces --if-present",
    "verify:all": "npm run verify --workspaces --if-present"
  }
}
